<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:c="http://www.springframework.org/schema/c"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:p="http://www.springframework.org/schema/p"
	xmlns:camel="http://camel.apache.org/schema/spring"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd">



	<bean id="acmq"
		class="org.apache.camel.component.jms.JmsComponent">
		<property name="connectionFactory">
			<bean id="cnf"
				class="org.apache.activemq.ActiveMQConnectionFactory"
				p:brokerURL="tcp://localhost:61616" />
		</property>
	</bean>



	<camel:camelContext>


		<camel:route>
			<camel:from
				uri="file:///Users/douglasdias/camel/input/full?noop=true" />
			<camel:to uri="acmq:queue:incomingOrders" />
		</camel:route>

		<camel:route>
			<camel:from uri="acmq:queue:incomingOrders" />
			<camel:choice>
				<camel:when>
					<camel:simple>${header.CamelFileName} ends with '.xml'</camel:simple>
					<camel:to uri="acmq:queue:xmlOrders" />
				</camel:when>
				<camel:when>
					<camel:simple>${header.CamelFileName} regex '^.*(csv|csl)$'</camel:simple>
					<camel:to uri="acmq:queue:csvOrders" />
				</camel:when>
				<camel:otherwise>
					<camel:to uri="acmq:queue:badOrders" />
				</camel:otherwise>
			</camel:choice>
		</camel:route>

		<camel:route>
			<camel:from uri="acmq:queue:xmlOrders" />
			<camel:multicast parallelProcessing="true">
				<camel:to uri="acmq:queue:accounting" />
				<camel:to uri="acmq:queue:production" />
			</camel:multicast>
		</camel:route>

		<camel:route>
			<camel:from uri="acmq:queue:accounting" />
			<camel:log
				message="Accounting received order: ${header.CamelFileName}" />
			<camel:to uri="mock:accounting" />

		</camel:route>

		<camel:route>
			<camel:from uri="acmq:queue:production" />
			<camel:log
				message="Accounting received order: ${header.CamelFileName}" />
			<camel:to uri="mock:production" />
		</camel:route>

	</camel:camelContext>



</beans>
