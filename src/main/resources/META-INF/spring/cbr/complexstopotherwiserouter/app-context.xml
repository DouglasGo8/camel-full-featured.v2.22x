<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:c="http://www.springframework.org/schema/c"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:p="http://www.springframework.org/schema/p"
	xmlns:camel="http://camel.apache.org/schema/spring"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd">



	<bean id="acmq"
		class="org.apache.camel.component.jms.JmsComponent">
		<property name="connectionFactory">
			<bean id="cnf"
				class="org.apache.activemq.ActiveMQConnectionFactory"
				p:brokerURL="tcp://localhost:61616" />
		</property>
	</bean>



	<camel:camelContext>


		<camel:route>
			<camel:from
				uri="file:///Users/douglasdias/camel/input/full?noop=true" />
			<camel:to uri="acmq:queue:incomingOrders" />
		</camel:route>

		<camel:route>
			<camel:from uri="acmq:queue:incomingOrders" />
			<camel:choice>
				<camel:when>
					<camel:simple>${header.CamelFileName} ends with '.xml'</camel:simple>
					<camel:to uri="acmq:queue:xmlOrders" />
				</camel:when>
				<camel:when>
					<camel:simple>${header.CamelFileName} regex '^.*(csv|csl)$'</camel:simple>
					<camel:to uri="acmq:queue:csvOrders" />
				</camel:when>
				<camel:otherwise>
					<camel:to uri="acmq:queue:badOrders" />
					<camel:stop />
				</camel:otherwise>
			</camel:choice>
			<camel:to uri="acmq:queue:continuedProcessing" />
		</camel:route>
		<camel:route>
			<camel:from uri="acmq:queue:xmlOrders" />
			<camel:log
				message="Received a XML order: ${header.CamelFileName}" />
			<camel:to uri="mock:xml" />
		</camel:route>
		<camel:route>
			<camel:from uri="acmq:queue:csvOrders" />
			<camel:log
				message="Received a CSV and CSL order: ${header.CamelFileName}" />
			<camel:to uri="mock:csv" />
		</camel:route>
		<camel:route>
			<camel:from uri="acmq:queue:badOrders" />
			<camel:log
				message="Received a BaD order: ${header.CamelFileName}" />
			<camel:to uri="mock:bad" />
		</camel:route>
		<camel:route>
			<camel:from uri="acmq:queue:continuedProcessing" />
			<camel:log
				message="Received a Continued order: ${header.CamelFileName}" />
			<camel:to uri="mock:bad" />
		</camel:route>
	</camel:camelContext>



</beans>
